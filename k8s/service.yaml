apiVersion: v1
kind: Service
metadata:
  name: vikareta-admin-service
  namespace: vikareta
spec:
  selector:
    app: vikareta-admin
  ports:
  - protocol: TCP
    port: 5751
    targetPort: 3002
  type: ClusterIP
